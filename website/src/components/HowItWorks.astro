<section id="how-it-works" class="how" aria-labelledby="how-heading">
  <div class="container">
    <header class="section-header">
      <p class="section-eyebrow">Under the hood</p>
      <h2 id="how-heading" class="section-title">How It Works</h2>
      <p class="section-desc">
        The safety pipeline sits between the user and the model.
        There is no path around it.
      </p>
    </header>

    <ol class="how__steps" aria-label="How snflwr.ai works">
      <li class="how__step animate-on-scroll" style="transition-delay: 0ms">
        <div class="how__step-num" aria-hidden="true">1</div>
        <div class="how__step-content">
          <h3 class="how__step-title">Student opens chat interface</h3>
          <p class="how__step-desc">
            Students interact with a polished AI tutor interface powered by Open WebUI.
            No accounts required for students — parents manage profiles.
          </p>
        </div>
      </li>

      <li class="how__step animate-on-scroll" style="transition-delay: 100ms">
        <div class="how__step-num" aria-hidden="true">2</div>
        <div class="how__step-content">
          <h3 class="how__step-title">Message passes through 5-stage safety pipeline</h3>
          <p class="how__step-desc">
            Every message is processed by: input validation → Unicode normalization →
            pattern matching → LLM classification → age-adaptive rules.
            If any stage errors, the content is blocked.
          </p>
          <ul class="how__pipeline" role="list" aria-label="Pipeline stages">
            <li class="how__pipeline-stage">
              <span class="how__pipeline-num" aria-hidden="true">①</span>
              Input validation
            </li>
            <li class="how__pipeline-stage">
              <span class="how__pipeline-num" aria-hidden="true">②</span>
              Unicode normalization
            </li>
            <li class="how__pipeline-stage">
              <span class="how__pipeline-num" aria-hidden="true">③</span>
              Pattern matching
            </li>
            <li class="how__pipeline-stage">
              <span class="how__pipeline-num" aria-hidden="true">④</span>
              LLM classification
            </li>
            <li class="how__pipeline-stage">
              <span class="how__pipeline-num" aria-hidden="true">⑤</span>
              Age-adaptive rules
            </li>
          </ul>
        </div>
      </li>

      <li class="how__step animate-on-scroll" style="transition-delay: 200ms">
        <div class="how__step-num" aria-hidden="true">3</div>
        <div class="how__step-content">
          <h3 class="how__step-title">AI processes locally via Ollama</h3>
          <p class="how__step-desc">
            The message is sent to a local Ollama instance running Qwen3.5 (or any
            compatible model). Nothing leaves your machine. GPU acceleration is
            auto-detected at setup.
          </p>
        </div>
      </li>

      <li class="how__step animate-on-scroll" style="transition-delay: 300ms">
        <div class="how__step-num" aria-hidden="true">4</div>
        <div class="how__step-content">
          <h3 class="how__step-title">Filtered, safe response returned</h3>
          <p class="how__step-desc">
            The AI response also passes through the safety pipeline before the student
            sees it. Parents receive real-time alerts for any flagged content, with
            full conversation history available in the dashboard.
          </p>
        </div>
      </li>
    </ol>
  </div>
</section>

<style>
/* ================================================================
   HOW IT WORKS
================================================================ */
.how {
  padding: 6rem 0;
  background: var(--gray-50);
}

.how__steps {
  list-style: none;
  max-width: 720px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 0;
}

.how__step {
  display: flex;
  gap: 1.5rem;
  position: relative;
}

.how__step:not(:last-child)::after {
  content: '';
  position: absolute;
  left: 1.25rem;
  top: 3.25rem;
  bottom: 0;
  width: 2px;
  background: linear-gradient(to bottom, var(--yellow), var(--green));
  opacity: 0.3;
}

.how__step:not(:last-child) {
  padding-bottom: 2.5rem;
}

.how__step-num {
  flex-shrink: 0;
  width: 2.5rem;
  height: 2.5rem;
  background: var(--yellow);
  color: var(--dark);
  font-weight: 800;
  font-size: 1rem;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  z-index: 1;
  box-shadow: 0 0 0 4px var(--yellow-light);
}

.how__step-content {
  padding-top: 0.4rem;
  flex: 1;
}

.how__step-title {
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--dark);
  margin-bottom: 0.5rem;
}

.how__step-desc {
  font-size: 0.9rem;
  color: var(--gray-600);
  line-height: 1.65;
  margin-bottom: 1rem;
}

.how__pipeline {
  list-style: none;
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-top: 0.75rem;
}

.how__pipeline-stage {
  display: flex;
  align-items: center;
  gap: 0.4rem;
  background: var(--white);
  border: 1px solid var(--gray-200);
  border-radius: 999px;
  padding: 0.25rem 0.75rem;
  font-size: 0.8rem;
  font-weight: 500;
  color: var(--gray-700);
}

.how__pipeline-num {
  color: var(--yellow-dark);
  font-weight: 700;
}
</style>
