<section class="cta" aria-labelledby="cta-heading">
  <div class="container cta__inner">
    <h2 id="cta-heading" class="cta__title">
      Ready to deploy safe AI?
    </h2>
    <p class="cta__desc">
      Open source under AGPL-3.0. 8 GB RAM recommended, 10 GB free disk, Docker Desktop.
    </p>
    <div class="cta__actions">
      <a
        href="https://github.com/snflwr-ai/snflwr.ai"
        class="btn btn--primary btn--lg"
        target="_blank"
        rel="noopener noreferrer"
      >
        Get started on GitHub
      </a>
      <a
        href="https://snflwr-ai.github.io/snflwr.ai/"
        class="btn btn--ghost btn--lg"
        target="_blank"
        rel="noopener noreferrer"
      >
        Read the docs
      </a>
    </div>
  </div>
</section>

<style>
/* ================================================================
   CTA SECTION
================================================================ */
.cta {
  padding: 6rem 0;
  background: var(--yellow-light);
  text-align: center;
}

.cta__inner {
  max-width: 560px;
}

.cta__title {
  font-size: clamp(2rem, 4vw, 2.75rem);
  font-weight: 800;
  color: var(--dark);
  letter-spacing: -0.02em;
  margin-bottom: 1rem;
}

.cta__desc {
  font-size: 1rem;
  color: var(--gray-600);
  margin-bottom: 2rem;
}

.cta__actions {
  display: flex;
  gap: 0.75rem;
  justify-content: center;
  flex-wrap: wrap;
}

.cta :global(.btn--primary) {
  background: var(--dark);
  border-color: var(--dark);
  color: var(--white);
}

.cta :global(.btn--primary:hover) {
  background: var(--gray-700);
  border-color: var(--gray-700);
}

.cta :global(.btn--ghost) {
  color: var(--dark);
  border-color: var(--gray-400);
}

.cta :global(.btn--ghost:hover) {
  background: rgba(0,0,0,0.05);
}
</style>
