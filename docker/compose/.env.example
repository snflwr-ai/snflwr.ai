# snflwr.ai - Docker Compose Environment
# ============================================
# Copy to .env and fill in values before running:
#   cp .env.example .env
#   docker compose up -d
#
# Or use the setup script to generate everything:
#   python ../../scripts/setup_production.py

# ── Required secrets (generate before first run) ──────────────────────
# python -c 'import secrets; print(secrets.token_hex(32))'
POSTGRES_PASSWORD=
POSTGRES_SSLMODE=require
REDIS_PASSWORD=
JWT_SECRET_KEY=
INTERNAL_API_KEY=
WEBUI_SECRET_KEY=

# ── AI models (required) ─────────────────────────────────────────────
# Chat model sized to your server RAM (see enterprise/build.sh --help)
CHAT_MODEL=qwen3.5:9b
# Safety classifier (mandatory for K-12)
SAFETY_MODEL=llama-guard3:1b

# ── Optional ─────────────────────────────────────────────────────────
LOG_LEVEL=INFO
ENABLE_SIGNUP=false

# CORS (set to your domain in production)
CORS_ORIGINS=https://localhost

# Email notifications (for parent safety alerts)
SMTP_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
ADMIN_EMAIL=
